# Enable continuous reverse history search with up arrow
bind '"\e[A": history-search-backward'

# Enable continuous reverse search forward with down arrow
bind '"\e[B": history-search-forward'

export EDITOR=vim
